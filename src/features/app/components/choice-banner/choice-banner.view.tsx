import type { ReactElement } from 'react';
import ActionBanner from '../../../../components/action-banner';

export default function ChoiceBanner(): ReactElement {
  return (
    <ActionBanner>
      <p>Choose a plane.</p>
    </ActionBanner>
  );
}
